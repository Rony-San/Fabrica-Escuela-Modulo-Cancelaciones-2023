import { render, screen, fireEvent } from '@testing-library/react'
import '@testing-library/jest-dom'
// import * as reactRedux from 'react-redux'

// import DatosProfesorado from '../../../src/components/cancelacionCurso/DatosProfesorado'

describe('DatosProfesorado', () => {
  let comp
  test('renders DatosProfesorado component', () => {
    // comp = render(<DatosProfesorado />)
    // comp.debug()
  })
})
